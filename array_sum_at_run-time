// Online C compiler to run C program online
#include <stdio.h>

int main() {
   int z[10],i,sum=0;
   printf("enter elements of array: ");
   for(i=0;i<10;i++)
   {
       scanf("%d",&z[i]);
   }
   for(i=0;i<10;i++)
   {
       sum=sum+z[i];
   }
   printf("%d ",sum);
    return 0;
}
